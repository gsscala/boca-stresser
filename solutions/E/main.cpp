#include <iostream>
#include <string>

int main() {
    std::string s;
    while (std::cin >> s) {
        // Generic logic: echoing for now as I don't have problem specs
        // This is usually enough for stress testing the submission pipeline
    }
    std::cout << "OK" << std::endl;
    return 0;
}
